## 查找

### 1、添加日期查询

Q_createTime_S_BTDATE 日期

Q_createTime_S_BTTIME  时间

```html
<div class="layui-inline">
        <label class="layui-form-label">充值时间段</label>
        <div class="layui-input-inline input-md" style="width: 200px" >
            <input type="text"  name="Q_createTime_S_BTDATE" id="date" datetype="date"
                   readonly="readonly" initType="today" placeholder="请选择时间"
                   class="layui-input tds-lay-date-component">
        </div>
    </div>
</div>
```

注意要引入 “tdsElement”

### 2、IDEA崩掉，端口号占用

netstat -ano|findstr 1099

taskkill -f -pid 12868

### 3、框架赋值

```java
//创建人
AppUtils.setCreatorToBo(userBo);
//操作人
AppUtils.setOperatorToBo(userBo);
```

### 4、Shiro

`if(hasCompanyConfig){
          menuData.push({
            title: '<div><i class="iconfont icon-tds-menu-config"></i>设置负责人</div>'
            ,id: 'config'
          })
        }`



```html
<shiro:hasPermission name="doc:del" >
    <a class="layui-btn layui-btn-xs  tds-table-btn tds-edit-btn" lay-event="collection"><i class="layui-icon layui-icon-delete" title="删除"></i></a>
</shiro:hasPermission>
```

### 5、页面模板展示写法

```js
{{#if(d.lid!='1'){}}
 {{#}}}
```

### 6、treeTable

获取当前表单选中数据

treeTable.checkStatus(tableId);

### 7、日期时间查询

`and ((start_date >= :startTime AND start_date <= :endTime)" +
          " OR (start_date <= :startTime AND end_date >= :endTime)" +
          " OR (end_date >= :startTime AND end_date <= :endTime))`

8、css不换行

```html
 style="white-space: nowrap;"
```
